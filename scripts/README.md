# Development Scripts

Use this folder for ROBOT workflows or other helper scripts that build, validate, or visualize the ontology.

## Available scripts

- `import_duo.sh` downloads the DUO ontology into `ontology/imports/duo.owl` so that downstream builds can rely on a vendored copy.
