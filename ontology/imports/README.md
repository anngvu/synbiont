# External Imports

This stores vendored ontologies or external Turtle files here so the build pipeline can include stable copies without fetching them on every run. Name files after the source ontology and record the upstream version in the file header comment.
